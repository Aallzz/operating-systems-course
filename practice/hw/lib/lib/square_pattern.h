#ifndef MY_SQUARQ_PATTERN
#define MY_SQUARQ_PATTERN 

#include <unistd.h>
#include <iostream>
#include <string>
#include <cassert>

std::ostream& print_square(std::ostream& out, int x = 3);
std::ostream& clear_square(std::ostream& out);
#endif
